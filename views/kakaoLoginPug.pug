<!DOCTYPE html>
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title Hey, KakaO!
    body
        script.
            window.onload = () => {
                var r = JSON.stringify({'at' : '#{at}', 'rt' : '#{rt}'});
                james.postMessage(r);
                //- setTimeout(
                //-     () => {
                //-         james.postMessage(r);
                //-     },
                //-     200
                //- );
            }