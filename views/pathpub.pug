<!DOCTYPE html>
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")   
        title #{index} : TITLE
    body
        header
            include ./headpug.pug 
        
        ul
            each i in data
                li 
                    p 
                        a(href=`/renderHtml/${i}`) #{i} : 데이터
        footer
            include ./footerpug.pug